package utils

// // FlatGroupsToTree converts an array of groups to build a tree
// func FlatGroupsToTree(groups []*models.Group) models.Group {
// 	var rootItems []models.Group

// 	lookup := models.Group

// 	for _, g := range groups {
// 		itemID := g.ID
// 		parentID := g.ParentID

// 		if !lookup.
// 	}

// }
